# Parser — pet‑проект (Flutter + Django + Docker)

Приложение на **Flutter** (интерфейс + отдельная админка) и сервер на **Django** (REST API).
Всё запускается через **Docker**, чтобы не ставить вручную кучу зависимостей.

## Что внутри
- **parser_app/** — фронт (Flutter). Там же админка.
- **prototype/** — сервер (Django). Отдаёт API.
- **docker/** — файлы для сборки контейнеров и настройки nginx.
- **docker-compose.yml** — общий запуск всего проекта одной командой.

## Как запустить

Перед запуском через Docker желательно собрать web‑версию:

```bash
cd parser_app
flutter pub get
flutter build web --release
cd ..
```

Если Flutter не трогали — можно пропустить этот шаг.

```bash
docker compose up -d --build
```

Посмотреть логи (если что-то не стартует):
```bash
docker compose logs -f
```

Остановить проект:
```bash
docker compose down
```

- Приложение (Web): http://localhost/
- API (сервер): http://localhost/api/

## Что уже сделано
- Рабочий интерфейс (минималистичный, с анимациями)
- Кастомная админка на Flutter
- Сервер на Django (REST API)
- Всё упаковано в Docker (поднимается одной командой)

## Что осталось доделать (планы)
- В админке: редактирование логинов/паролей пользователей
- В админке: загрузка каталога файлов
- Навести порядок в файлах Flutter‑проекта
- Чуть лучше подогнать интерфейс под разные размеры экранов
